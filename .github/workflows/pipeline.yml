name: ITSE Supply Chain Pipeline

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  build-sign-push:
    runs-on: ubuntu-latest

    env:
      IMAGE_TAG: ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com/${{ secrets.ECR_REPO }}:${{ github.sha }}

    steps:
    - name: Checkout source code
      uses: actions/checkout@v3

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ secrets.AWS_REGION }}

    - name: Login to Amazon ECR
      uses: aws-actions/amazon-ecr-login@v2

    # -------------------------
    # Install Syft (latest)
    # -------------------------
    - name: Install Syft (latest)
      run: |
        curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh \
          | sh -s -- -b /usr/local/bin latest
        syft version

    # -------------------------
    # Install Trivy
    # -------------------------
    - name: Install Trivy
      run: |
        sudo apt-get update
        sudo apt-get install -y wget gnupg lsb-release
        wget https://aquasecurity.github.io/trivy-repo/deb/public.key -O - \
          | sudo gpg --dearmor -o /usr/share/keyrings/trivy.gpg
        echo "deb [signed-by=/usr/share/keyrings/trivy.gpg] https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main" \
          | sudo tee /etc/apt/sources.list.d/trivy.list > /dev/null
        sudo apt-get update
        sudo apt-get install -y trivy

    # -------------------------
    # Install Cosign
    # -------------------------
    - name: Install Cosign
      run: |
        curl -sSfL https://github.com/sigstore/cosign/releases/latest/download/cosign-linux-amd64 \
          -o /usr/local/bin/cosign
        chmod +x /usr/local/bin/cosign
        cosign version

    # -------------------------
    # Generate SBOM (valid SPDX JSON)
    # -------------------------
    - name: Generate SBOM (SPDX JSON)
      run: |
        syft app/ -o spdx-json=sbom.json
        echo "Generated SBOM:"
        head -n 20 sbom.json

    # -------------------------
    # Build Docker Image
    # -------------------------
    - name: Build Docker image
      run: docker build -t $IMAGE_TAG ./app

    # -------------------------
    # Vulnerability Scan
    # -------------------------
    - name: Trivy Vulnerability Scan
      run: |
        trivy image --exit-code 1 --severity HIGH,CRITICAL $IMAGE_TAG

    # -------------------------
    # Push Image + Capture Digest
    # -------------------------
    - name: Push Image to ECR and capture digest
      id: push-image
      run: |
        docker push $IMAGE_TAG
        DIGEST=$(docker inspect --format='{{index .RepoDigests 0}}' $IMAGE_TAG)
        echo "DIGEST=$DIGEST" >> $GITHUB_ENV
        echo "Image pushed with digest: $DIGEST"

    # -------------------------
    # Import Cosign Key
    # -------------------------
    - name: Import Cosign private key
      run: |
        echo "${{ secrets.COSIGN_KEY }}" > cosign.key
        chmod 600 cosign.key

    # -------------------------
    # Sign Image
    # -------------------------
    - name: Sign Image with Cosign (using digest)
      run: |
        cosign sign --key cosign.key $DIGEST --yes

    # -------------------------
    # Create DSSE Attestation Envelope (Cosign generates correct in-toto format)
    # -------------------------
    - name: Generate SBOM Attestation Envelope
      run: |
        cosign attest generate \
          --predicate sbom.json \
          --type spdx \
          --key cosign.key \
          --yes \
          --output-file sbom.att.json \
          $DIGEST
        echo "Generated attestation envelope:"
        head -n 20 sbom.att.json

    # -------------------------
    # Attach Attestation to Image
    # -------------------------
    - name: Attach SBOM Attestation
      run: |
        cosign attach attestation \
          --key cosign.key \
          --attestation sbom.att.json \
          --yes \
          $DIGEST

    # -------------------------
    # Verify Final Signature
    # -------------------------
    - name: Verify Signature (using digest)
      run: |
        cosign verify --key cosign.pub $DIGEST
